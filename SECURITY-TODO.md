# SECURITY-TODO — فجوات تحتاج قرار المالك (توثيق فقط، بلا تنفيذ)

هذه القائمة تسرد فجوات أمنية/تشغيلية كبرى **لم** تُطبَّق في جولة التنظيف الآمن
لأنها قد تكسر التشغيل أو تحتاج قرار مالك/بنية خادمية. كلها **توثيق فقط**.

## 1. واجهة الإنتاج تعرض بيانات Seed
- `src/lib/store.ts:66-108`: بذور بيانات (عملاء/طلبات/كيانات) مُحمّلة في حالة العميل ضمن الإنتاج.
- `clearSeedDomainData` فارغة (لا تمسح شيئاً) — البذور تبقى ظاهرة للمستخدم النهائي.
- القرار المطلوب: فصل البذور عن بناء الإنتاج (علم بيئة / حذف عند الإقلاع).

## 2. إجراءات الأدمن لا تصل للخادم
- `src/lib/store.ts:382-420`: إجراءات الأدمن تُنفَّذ على الحالة المحلية فقط ولا تُكتب للخادم.
- لا كتابة خادمية موثوقة للطلبات / المستحقات (payouts) / التقييمات — تُفقد أو تُزوَّر من العميل.
- القرار المطلوب: نقاط نهاية خادمية موثّقة + تحقّق صلاحيات.

## 3. توقيع العقد معطّل (PACI)
- تكامل PACI للتوقيع الرقمي معطّل (disabled) — لا توقيع قانوني فعلي على العقود.
- القرار المطلوب: تفعيل PACI أو مزوّد توقيع بديل قبل أي التزام قانوني.

## 4. استعادة كلمة المرور بلا قناة تسليم
- لا قناة (بريد/SMS) لتسليم رمز الاستعادة للمستخدم فعلياً.
- رمز الاستعادة يُولَّد عبر `Math.random` — غير آمن تشفيرياً (يجب `crypto` آمن).
- القرار المطلوب: مزوّد بريد/SMS + مولّد رموز آمن.

## 5. كود API ميت
- ~2500 سطر من كود API غير مستخدم/ميت (لم يُحذف في هذه الجولة لتجنّب كسر البناء غير المباشر).
- القرار المطلوب: مراجعة وحذف مُوجَّه بعد التأكد من عدم الاعتماد وقت التشغيل.

## 6. قواعد Firestore — قراءة واسعة
- `firestore.rules:15`: صلاحية قراءة واسعة (`allow read: if isAuthenticated()` / أوسع) تكشف بيانات أكثر من اللازم.
- القرار المطلوب: تضييق القراءة حسب الملكية/الدور.

## 7. انحراف firebase-blueprint.json عن firestore.rules
- `firebase-blueprint.json` يحمل قواعد **أضعف** من `firestore.rules`:
  يمنح `write: request.auth != null` على `creators`/`hosts`/`products` (أي مستخدم مسجّل يكتب أي وثيقة)،
  بينما `firestore.rules` تقيّد الكتابة على المالك (`request.auth.uid == request.resource.data.userId`).
- لم يُحذف الملف. القرار المطلوب: مواءمة أو حذف الـblueprint حتى لا يُنشر بالخطأ كمصدر قواعد.

## 8. Rate limit في الذاكرة + TRUST_PROXY_HOPS=0
- حد المعدّل (rate limit) مخزّن في الذاكرة — يُفقد عند إعادة التشغيل ولا يصمد أفقياً (multi-instance).
- `TRUST_PROXY_HOPS=0` مع وجود بروكسي = IP العميل غير موثوق، يبطل فعالية الحد.
- القرار المطلوب: مخزن مشترك (Redis) + ضبط عدد قفزات البروكسي الصحيح.

## 9. تنظيف تاريخ git (يحتاج موافقة منفصلة)
- جولة التنظيف حذفت ملفات من الشجرة فقط (commits جديدة). لم يُعَد كتابة التاريخ.
- لم تُحذف أي ملفات PII في هذه الجولة (لم يُعثر على ملفات JSON/CSV تحوي بيانات حقيقية).
- ملاحظة: `firebase-applet-config.json` (المحذوف) كان يحمل إعداد Firebase عام؛
  إن اعتُبر أي مفتاح حسّاساً فتنظيف التاريخ يحتاج موافقة المالك.

## 10. تبعيات غير مستخدمة في package.json (لم تُحذف)
- `firebase` (`^12.17.1`): بعد حذف `src/lib/firebase.ts` و`src/lib/cloudSync.ts` لم يعد مستورَداً في الكود.
- `motion` (`^12.23.24`): غير مستورَد في أي مكان.
- لم تُحذف من `package.json` تجنّباً لاعتماد بناء غير مباشر. القرار المطلوب: التحقق ثم الإزالة.
