node_modules
dist
.git
.github
.env*
!*.example
coverage
*.log
.DS_Store
*.md

# لا تستبعد scripts/: البناء يحتاجه مرّتين — `npm run build` يبدأ بـ
# scripts/verify-pwa.mjs، و vite.config.ts يستورد ./scripts/build-stamp.mjs.
# استبعاده يُفشل بناء الصورة بـ "Cannot find module"، ولا يظهر محلياً لأن المجلد
# موجود هناك. لا يكلّف شيئاً في الصورة النهائية: المرحلة الثانية تنسخ dist فقط.

