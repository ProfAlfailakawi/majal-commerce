# MAJAL — Design System

## الهوية والعلامة

العلامة رسم هندسي، لا حرف مكتوب بخط النظام. تتكوّن من:

- **البوابة (القوس)** — «مجال» كبيئة مرخّصة يمر المنتج عبرها.
- **الحلقة** — فكرة المبدع محفوظة داخل البوابة، وتحمل في الوقت نفسه حلقة حرف «م».
- **خط الأرض** — يُستخدم في المقاسات الكبيرة والاحتفالية فقط (Splash، Hero، OG).

التدرّج الذهبي يسير من اليمين (غامق) إلى اليسار (فاتح)، أي باتجاه القراءة، فتشير العلامة
نفسها إلى اتجاه الرحلة.

قواعد الاستخدام:

- المصدر الوحيد للعلامة هو `src/components/brand/MajalMark.tsx`. لا تُرسم يدويًا في أي مكان.
- كل الحدود تحمل `pathLength="1"`، فأي حركة رسم تُنفَّذ بـ dashoffset واحد بلا حساب أطوال.
- كلمة «مجال» لا تأخذ `letter-spacing` أبدًا — العربية متصلة، والتباعد يقطع الوصلات.
  التباعد يُستخدم فقط في السطر اللاتيني `MAJAL`.
- الأصول: `public/favicon.svg` (متصفح)، `public/apple-touch-icon.svg` (iOS، مساحة أوسع
  وخلفية معتمة لأن iOS لا يحترم الشفافية)، `public/og-cover.svg` (معاينة الروابط).

## شاشة الإقلاع (Splash)

مكتوبة داخل `index.html` نفسه — لا CSS خارجي ولا خط ولا سكربت — فتظهر الهوية في أول
رسم للصفحة بدل مستطيل أزرق فارغ حتى تحميل الحزمة.

- تُرفع من `src/main.tsx` بعد أول رسم فعلي (إطارين)، وبحد أدنى ~1.15 ثانية محسوبة من
  لحظة تحليل الـ HTML لا من لحظة تنفيذ الحزمة؛ الوميض القصير يُقرأ كخلل لا كهوية.
- شريط التقدم غير محدد النسبة عمدًا: اختراع نسبة مئوية لتحميل لا يبلّغ عن تقدّمه كذبة.
- مؤقّت أمان 8 ثوانٍ يرفعها مهما حدث، حتى لا تحبس زائرًا إذا فشلت الحزمة.

## التعريف الأول (Onboarding)

`src/components/onboarding/OnboardingExperience.tsx` — خمس شاشات تُرسّخ نموذج التشغيل
قبل تسليم المفاتيح: ترحيب، سؤال واحد عن الغرض، الرحلة بمحطاتها الست، سلّم الإفصاح
الثلاثي، ثم الانطلاق.

- سؤال واحد فقط، وتُستخدم إجابته فعليًا في تحديد الوجهة.
- قابل للتخطي من أول إطار، ولا يعود بعد إغلاقه (`majal_onboarding_v1`)، ويبقى قابلًا
  لإعادة التشغيل من الفوتر.
- لا يغيّر أي حالة عمل. الوجهة تمر عبر تبديل السطح نفسه المحكوم بالصلاحيات، فالزائر غير
  المسجّل الذي يختار «مبدع» يُوجَّه لتسجيل الدخول لا إلى بوابة ترفضه.
- نص الرحلة مصدره الوحيد `src/data/journey.tsx`، مشترك مع الإنفوجرافيك العام.

## الألوان

سلالم اللون معرّفة كـ tokens في `src/index.css` (`--gold-100…700`, `--ink-500…900`).
اختر الدرجة بدورها (خط، حافة، نص، تعبئة) لا بتقدير بصري لقيمة hex.

- Deep Navy / Ink: الخلفية والحوكمة.
- Majal Gold `#C7A55B` وSoft Gold `#E8C880`: الهوية والقرارات الأساسية.
- Sky: البيانات والتشغيل.
- Emerald: النجاح والجودة/الجاهزية.
- Fuchsia/Violet: طبقة السوبر أدمن والذكاء/الاستراتيجية.
- Rose: الخطر فقط، ولا يستخدم للزينة.

## العمق (Elevation)

`--elev-1/2/3` و`--elev-gold`. كل مستوى يحمل ظلًا **وحافة علوية مضيئة** (`--edge-hi`):
على خلفية داكنة، الحافة المضيئة هي ما يجعل اللوح يبدو سطحًا ماديًا لا مستطيلًا أفتح.

## الشخصية

حديثة، كويتية من غير فلكلور مباشر، إنسانية للمبدع، مؤسسية للمنشأة، ومركزة على القرار للإدارة.

## قواعد الواجهة

- RTL أولًا.
- كل زر أساسي له فعل واحد واضح.
- لا تستخدم اللون وحده لشرح الحالة؛ النص/الأيقونة يرافقانه.
- الأسرار المالية والوصفات تعرض حسب الدور والسياق.
- العمليات الخطرة لا تخفى بصريًا فقط؛ Domain Guard يمنعها أيضًا.
- Cards ذات زجاج خفيف، لكن النصوص المهمة على خلفية عالية التباين.
- لا تستخدم Emojis داخل واجهة النظام التشغيلية.

## مقاسات العناصر التفاعلية

كان في التطبيق **٢٧ مقاسًا مختلفًا** لـ ٩٨ زرًا وشارة، لأنه ما كان في مفردات مقاس —
فكل زر يُقدَّر بالعين وقت كتابته. المقاسات الآن خمسة، ولكل واحد معنى:

| الاستخدام | المقاس |
|---|---|
| زر ثانوي/مضغوط | `px-3 py-2 rounded-xl` |
| زر أساسي | `px-4 py-2.5 rounded-xl` |
| زر أساسي عريض | `px-6 py-2.5 rounded-xl` |
| CTA بارز | `px-6 py-3.5 rounded-2xl` |
| شارة | `px-2.5 py-1 rounded-full` أو `px-3 py-1.5 rounded-full` للأطول |

`rounded-xl` للتحكّم المضغوط و`rounded-2xl` للأسطح البارزة. أي مقاس خارج الجدول يعني
إمّا حالة استثنائية موثّقة، أو انزلاق يُعاد للمقياس.

## اللغة

المنتج عربي أولًا. **العناوين تُكتب بالعربية دائمًا** — العنوان أول ما تمسحه العين،
وعنوان إنجليزي في واجهة عربية يضيع على القارئ. كانت ٢٤ سلسلة إنجليزية معروضة، منها
عناوين أقسام كاملة مثل «Governance Guardrails» و«Majal Admin Control Center».

الاستثناء الوحيد: **eyebrow** بأحرف كبيرة ومتباعدة فوق عنوان عربي (`MAJAL PULSE`,
`MAJAL JOURNEY`) — هذه تُقرأ كطباعة هوية لا كنص. وأسماء المنتجات التقنية تبقى كما هي
(Firestore) لأنها أسماء علم.

## الرموز

لا إيموجي في الواجهة التشغيلية، ولا رموز نصية (`✓` `✕`) مكان الأيقونات — مجموعة
lucide هي مصدر الأيقونات الوحيد، عشان الوزن البصري والحجم يظلّان متسقين.

## Motion

- منحنى واحد للدخول (`--ease-out`) وواحد للتحولات (`--ease-in-out`)، وثلاث مدد
  (`--dur-fast/base/slow`). خلط المنحنيات هو ما يجعل الواجهة تبدو مجمّعة من أيدٍ مختلفة.
- الحركة المستمرة الوحيدة المسموح بها هي الحقول المحيطة منخفضة التباين في الأسطح
  التعريفية (Splash، Onboarding، Hero) — ولا تدخل أبدًا أسطح القرار والإدارة.
- عند `prefers-reduced-motion` لا يكفي تصفير المدة: الرسم بـ dashoffset يتجمّد عندها عند
  قيمة 1 أي شعار غير مرئي، لذلك تُلغى الـ dashes ويُعرض الشكل النهائي مباشرة.
- انتقالات قصيرة وهادئة فقط.
- `prefers-reduced-motion` محترم عالميًا.
- لا توجد حركات مستمرة تشتت مستخدم الإدارة.

## الخلفيات الزخرفية والأداء

الخلفية المحيطة للتطبيق (`.ambient-bg`) تُرسم **بتدرّجات radial على عنصر واحد بلا أبناء**،
لا بدوائر `blur-3xl` داخل غلاف `opacity`.

السبب: الغلاف ذو الشفافية يجبر المجموعة كلها على buffer خارجي، وكل دائرة مموّهة تمريرة
filter مستقلة، ثم **كل لوح `backdrop-filter` في الشاشة مضطر يقرأ هذي الكومة كخلفية له**.
التكلفة تتضاعف بعدد الأسطح الزجاجية، ولذلك كانت تظهر كأشرطة فاضية أثناء التمرير في
الأسطح الكثيفة. تثبيت الطبقة في الذاكرة (المعالجة السابقة) كان يعالج العَرَض، والكومة
تظل تُقرأ.

الدائرة المموّهة غاوسيًا هي رياضيًا تدرّج شعاعي، فتُستنسخ بالضبط بـ:

    alpha(d) = base × ½ · (1 + erf((r − d) / (σ√2)))

مع `σ = 64px` — لأن `blur(64px)` في CSS تعني `stdDeviation=64` لا نصفها — وبطيّ شفافية
الغلاف داخل `base`. تحقّقنا من التطابق بمقارنة `mix-blend-mode: difference` بين الشكلين:
الفرق أقل من 0.3 من 255.

**القاعدة:** أي توهّج زخرفي يقع تحت أسطح زجاجية يُرسم تدرّجًا، لا دائرة مموّهة، ولا يُلفّ
أبدًا بغلاف `opacity`.

التوهّجات الباقية داخل أبطال الأسطح (`CreatorPortal`, `HostPortal`, `ConsumerDashboard`,
`SuperAdminDashboard`, `MajalPulse`, `PredictiveInterventionRadar`) بقيت دوائر مموّهة:
واحدة لكل سطح، ثابتة، وبلا غلاف شفافية — أي بلا العوامل الثلاثة المُفاقِمة. تُحوَّل عند
أي إعادة عمل عليها، لا قبلها.

## Responsive

- Desktop: لوحات القرار والتحليلات بكثافة أعلى.
- Tablet: شبكات 2-column مع بقاء CTA ظاهرًا.
- Mobile: صفوف تنقل قابلة للتمرير، cards عمودية، ولا تعتمد أي وظيفة على hover.

## حالات النظام

كل workflow إنتاجي يجب أن يملك: Empty / Loading / Success / Error / Permission denied / Suspended / Expired عند انطباقها. لا تعرض شاشة فارغة من غير تفسير وإجراء تالٍ.
